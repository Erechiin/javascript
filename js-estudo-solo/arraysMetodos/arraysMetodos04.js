// PREENCHER UMA ARRAY COM VALORES

// .fill( valor a ser botado, inicio(opcional), fim(opcional) )
    // Botando apenas o valor, vai preencher tudo com esse valor
    // Botando o valor e o inicio, vai preencher do inicio até o final da array
const arrPreenchido = Array(10)
arrPreenchido.fill(5,1)