const objEx = {
    100: 'a',
    5: 'b',
    67: 'c'
}

// Object.keys(obj)

const chaves = Object.keys(objEx)
console.log(chaves)

// Object.values(obj)

const valores = Object.values(objEx)
console.log(valores)